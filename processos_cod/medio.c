#include <stdio.h>
#include <time.h>

int main() {
    // time_t inicio = time(NULL);
 
    for(long int i=0; i<19999500000; i++);
 
    // time_t fim = time(NULL);
    // printf("Tempo de execucao: %lf segundos\n", (double) (fim - inicio));
 
    return 0;
}